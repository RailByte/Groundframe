<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="3.1.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\GroundFrame.Classes\GroundFrame.Classes.csproj" />
  </ItemGroup>

  <Target Name="PreBuild" BeforeTargets="PreBuildEvent">
    <Exec Command="@echo off&#xD;&#xA;SET json={&quot;gfSqlServer&quot;:&quot;(localdb)\\MSSQLLocalDB&quot;,&quot;gfDbName&quot;:&quot;GroundFrame.SQL&quot;, &quot;environment&quot;: &quot;localhost&quot;}&#xD;&#xA;if not exist &quot;$(ProjectDir)\queuer.localhost.config.json&quot; @echo %25json%25 &gt;&quot;$(ProjectDir)\queuer.localhost.config.json&quot;&#xD;&#xA;SET json=&quot;{&quot;environment&quot;: &quot;localhost&quot;}&#xD;&#xA;if not exist &quot;$(ProjectDir)\queuer.environment.json&quot; @echo %25json%25 &gt;&quot;$(ProjectDir)\queuer.environment.json&quot;&#xD;&#xA;" />
  </Target>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="xcopy /y &quot;$(ProjectDir)queuer.localhost.config.json&quot;  &quot;$(OutDir)&quot;&#xD;&#xA;xcopy /y &quot;$(ProjectDir)queuer.environment.json&quot;  &quot;$(OutDir)&quot;" />
  </Target>

</Project>
